# Azure Deployment Environment Variables
# Rename this file to .env and fill in your values

# Database (Azure PostgreSQL Flexible Server)
DATABASE_URL="postgresql://USERNAME:PASSWORD@SERVER.postgres.database.azure.com:5432/DATABASE?sslmode=require"

# NextAuth (required for sign-in)
# Production: use your public URL so callbacks and cookies work (e.g. https://aimediatank.com)
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="https://aimediatank.com"

# Azure Blob Storage
AZURE_STORAGE_CONNECTION_STRING="DefaultEndpointsProtocol=https;AccountName=xxx;AccountKey=xxx;EndpointSuffix=core.windows.net"
AZURE_STORAGE_CONTAINER_NAME="media"

# Stripe
STRIPE_SECRET_KEY="sk_live_xxx"
STRIPE_PUBLISHABLE_KEY="pk_live_xxx"
STRIPE_WEBHOOK_SECRET="whsec_xxx"

# Email (Gmail SMTP — requires a Google App Password, not the account password)
EMAIL_USER="your-email@gmail.com"
EMAIL_PASS="your-google-app-password"

# Optional: Cron Secret (for securing cron endpoints)
CRON_SECRET="your-cron-secret"

# Optional: Google AdSense
NEXT_PUBLIC_ADSENSE_CLIENT_ID="ca-pub-xxx"

# Optional: Kakao Talk Share (adds KakaoTalk to the media Share list)
# Get the JavaScript key from developer.kakao.com → your app → App → App key
NEXT_PUBLIC_KAKAO_JAVASCRIPT_KEY="your_javascript_key_here"








